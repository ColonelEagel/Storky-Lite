import { CourseForm } from "../components/courseForm";

function SingleCourse({params,
}: {
    params: { courseId: string };
}) {
  return (
    <>
      {/* header (title) + add session + edit session + add lesson  + edit course + invite student  */}


      {/* course content */}
        {/* Media content */}

        {/* sessions */}
          {/* lessons  */}

    </>
  );
}

export default SingleCourse;
